# Adapter: `physics_question_router`

This adapter is optional. Use rules first:

- Prefix `CHLT` or theory wording -> `conceptual_chlt`.
- Numeric target with physical units -> `numeric_calculator`.
- Options present -> conceptual/type1 route.

Train this only if rule routing creates many errors.

